import Head from "next/head";
import Image from "next/image";
import Layout from "./../components/Layout";
import MainSection from "./../components/MainSection";
import { getHomePageData } from "../lib/content";

export default function Home({ data }) {
  return (
    <Layout>
      <Head>
        <title>Jej wnętrza</title>
        <meta name="description" content="Generated by create next app" />
      </Head>
      <main className="text-blue-dark h-screen grid grid-cols-1 md:grid-cols-main-wide">
        <MainSection id="intro"> 
          <h1>{JSON.stringify(data)}</h1>
          <Image src={data.logo} height={500} width={500} alt="logo" />
        </MainSection>
        <MainSection id="offer1">
          <h1 className="text-5xl text-center mb-10">Projektowanie wnętrz</h1>
          <section>Lorem ipsum dolor sit amet, consectetur adipiscing elit</section>
        </MainSection>
      </main>
    </Layout>
  );
}

export async function getStaticProps(context) {
  const data = await getHomePageData();
  return {
    props: { data },
  };
}
